---
import BaseLayout from "../layouts/BaseLayout.astro";
import NewsCard from "../components/news/NewsCard.astro";
import TrendingTopics from "../components/news/TrendingTopics.astro";
import { getArticlesByCategory, getTrendTopics, getCategoryCount } from "../lib/data";

const articles = getArticlesByCategory("tech", 50);
const trendTopics = getTrendTopics();
---

<BaseLayout title="Tech">
  <div class="mb-8">
    <h1 class="text-xs font-semibold tracking-wide uppercase text-[var(--text-tertiary)] mb-2">💻 Tech</h1>
    <p class="text-[var(--text-secondary)] text-sm">AI, startups, gadgets, software, and big tech ({getCategoryCount("tech")} articles)</p>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-[1fr_280px] gap-8">
    <div class="space-y-3">
      {articles.map((article) => (
        <NewsCard {...article} publicationDate={new Date(article.publicationDate)} />
      ))}
    </div>
    <aside class="max-lg:hidden">
      <div class="sticky top-24">
        <TrendingTopics trends={trendTopics.slice(0, 10)} period="trending" />
      </div>
    </aside>
  </div>
</BaseLayout>
