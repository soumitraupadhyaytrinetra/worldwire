---
import { timeAgo } from "../../lib/utils";

export interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="text-xs text-[var(--text-tertiary)]">
  {timeAgo(date)}
</time>
